//Question: Implement a function called duplicateCount that takes a string as input and returns the count of characters that appear more than once. Ignore case sensitivity. For example, given the input "aabbcA", the function should return 2 because 'a' and 'b' both appear more than once.